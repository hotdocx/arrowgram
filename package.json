{
  "name": "arrowgram-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.6.2",
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w packages/web",
    "build": "npm run build --workspaces",
    "test": "npm run check:fast",
    "check:fast": "npm run check:fast -w packages/arrowgram && npm run build -w packages/arrowgram && npm test -w packages/web && npm run build -w packages/agent && npm test -w packages/agent && npm run validate:paper -w packages/paged",
    "check": "npm run check:fast && npm run check -w packages/arrowgram && npm run build -w packages/web && npm run build -w packages/paged && npm run test:packed -w packages/agent",
    "check:e2e": "npm run test:e2e -w packages/web && npm run test:e2e -w packages/agent",
    "check:full": "npm run check && npm run check:e2e",
    "deploy:lastrevision:azure": "bash scripts/deploy_lastrevision_azure.sh",
    "migrate:lastrevision:gcs-to-azure": "tsx scripts/migrate_lastrevision_gcs_to_azure_blob.ts"
  },
  "devDependencies": {
    "eslint": "^10.9.1"
  }
}
